import 'package:flutter/material.dart';

class LogListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Log List Screen')),
    );
  }
} 